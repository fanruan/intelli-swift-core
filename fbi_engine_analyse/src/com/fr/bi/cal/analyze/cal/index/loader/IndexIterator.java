package com.fr.bi.cal.analyze.cal.index.loader;import com.finebi.cube.api.ICubeDataLoader;import com.finebi.cube.conf.table.BusinessTable;import com.fr.bi.stable.report.result.DimensionCalculator;import java.util.Iterator;/** * Created by Hiram on 2015/1/15. */public class IndexIterator implements Iterator {    private final DimensionCalculator column;    private final BusinessTable tableKey;    private final ICubeDataLoader loader;    private int[] sortIndex = new int[]{2, 3, 4, 5, 6};    private Iterator iterator;    private int pos = 0;    public IndexIterator(Iterator iterator, DimensionCalculator column, BusinessTable tableKey, ICubeDataLoader loader) {        this.column = column;        this.tableKey = tableKey;        this.loader = loader;    }    @Override    public boolean hasNext() {        return pos < sortIndex.length;    }    @Override    public Object next() {        pos++;        return iterator.next();    }    @Override    public void remove() {    }}