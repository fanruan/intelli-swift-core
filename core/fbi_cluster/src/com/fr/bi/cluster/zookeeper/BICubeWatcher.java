package com.fr.bi.cluster.zookeeper;import com.fr.bi.cluster.ClusterAdapter;import org.apache.zookeeper.WatchedEvent;/** * Created by Hiram on 2015/2/26. */public class BICubeWatcher extends BIWatcher {    public final static String CONFIG_UPDATE_NODE = "/config/update";    public BICubeWatcher() {    }    @Override    public String getFocusedEventPath() {        return CONFIG_UPDATE_NODE;    }    @Override    public void eventProcessor(WatchedEvent event) {        if (!ClusterAdapter.getManager().getHostManager().isSelf()) {//            System.out.println("更新配置");//            ClusterAdapter.getManager().getClusterConfigManager().updateConfig();        }    }}