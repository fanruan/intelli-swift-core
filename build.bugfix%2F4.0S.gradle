apply plugin: 'java'
def webName="nuclear-web"
def branchName = "bugfix%2F4.0S"
def buildDir="../../${webName}/${branchName}"

task copyModel{
	ant{
		mkdir(dir:"${buildDir}")
		copy(todir:"${buildDir}"){
			fileset(dir:"./"){
				include(name:"**/**")
			}
		}
	}
}