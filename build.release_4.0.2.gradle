apply plugin: 'java'
def webName="nuclear-web"
def branchName = "release/4.0.2"
def buildDir="../../${webName}/${branchName}"

task copyModel{
	ant{
		mkdir(dir:"${buildDir}")
		copy(todir:"${buildDir}"){
			fileset(dir:"./"){
				include(name:"**/**")
			}
		}
	}
}